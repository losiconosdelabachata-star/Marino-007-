# Pinned to what is running locally, so the container behaves identically.

anthropic==0.120.2
requests==2.33.0
python-dotenv==1.2.2
schedule==1.2.2

# Google Photos / Drive
google-auth==2.56.2
google-auth-oauthlib==1.4.0
google-auth-httplib2==0.4.0
google-api-python-client==2.198.0
